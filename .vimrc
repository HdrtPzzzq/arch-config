" Represent trail space and tabs by dot.
set list
set listchars=trail:.

" Disable beeping sounds on vim
set belloff=all

" Indentation settings for using hard tabs for indent. Display tabs as four
" Characters wide.
set tabstop=4
set shiftwidth=4

" Indentation settings for using 4 spaces instead of tabs.
" Do not change'tabstop' from its default value fo 8 with this setup.
set expandtab
set softtabstop=4

" Display line numbers on the left
set number

" Enable use of the mouse for all modes
set mouse=a